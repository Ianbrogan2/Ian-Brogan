---
import type { InvestingData } from '../lib/content';
interface Props { data: InvestingData; }
const { data } = Astro.props;
---

<section id="investing" class="section" aria-labelledby="investing-heading">
  <div class="container">
    <div class="investing-layout">
      <div class="investing-text">
        <h2 id="investing-heading">{data.headline}</h2>
        <p class="lead">{data.subhead}</p>

        <div class="investing-body">
          {data.body.map((para) => (
            <p>{para}</p>
          ))}
        </div>

        <p class="career-note">{data.career_note}</p>
      </div>

      <aside class="investing-philosophy" aria-label="Investment philosophy">
        <h3>Philosophy</h3>
        <ul role="list">
          {data.philosophy.map(item => (
            <li>
              <span class="phil-dash" aria-hidden="true">—</span>
              <span>{item}</span>
            </li>
          ))}
        </ul>
      </aside>
    </div>
  </div>
</section>

<style>
  #investing { background: var(--color-surface); position: relative; overflow: hidden; }

  #investing::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(237,232,223,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(237,232,223,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
  }

  .investing-layout {
    position: relative;
    display: grid;
    grid-template-columns: 3fr 2fr;
    gap: clamp(3rem, 6vw, 7rem);
    align-items: start;
  }

  h2 { margin-bottom: 1.25rem; color: var(--color-text); }
  .lead { margin-bottom: 2rem; color: var(--color-text-dim); }

  .investing-body p {
    color: var(--color-text-dim);
    line-height: 1.75;
    margin-bottom: 1em;
  }

  .career-note {
    margin-top: 2rem;
    padding: 1.25rem 1.5rem;
    border-left: 3px solid var(--color-gold);
    background: rgba(201, 162, 39, 0.06);
    font-size: 0.9rem;
    color: var(--color-text-dim);
    font-style: italic;
    max-width: 55ch;
  }

  .investing-philosophy {
    padding: 2rem;
    background: var(--color-elevated);
    border: 1px solid var(--color-border);
  }
  .investing-philosophy h3 {
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-gold);
    margin: 0 0 1.25rem;
    font-family: var(--font-body);
    font-weight: 600;
  }
  .investing-philosophy ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .investing-philosophy li {
    display: flex;
    gap: 0.75rem;
    font-size: 0.875rem;
    line-height: 1.55;
    color: var(--color-text-dim);
    border-bottom: 1px solid var(--color-border);
    padding-bottom: 1rem;
  }
  .investing-philosophy li:last-child { border: none; padding: 0; }
  .phil-dash {
    color: var(--color-gold);
    font-weight: 700;
    flex-shrink: 0;
    font-family: var(--font-display);
  }

  @media (max-width: 900px) {
    .investing-layout { grid-template-columns: 1fr; }
    .investing-philosophy { order: -1; }
  }
</style>
