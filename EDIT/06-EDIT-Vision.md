# Vision Page (/vision) — Editing Guide

## Sections on this page
Controlled by: `content/vision.yaml`

### 1. Top headline
- `headline` and `subhead` — safe to edit freely.

### 2. "Where I'm applying" (target schools)
- Each school has a `name`, `program`, and `note`.
- Safe to change: any of these, or add/remove a school by copying the format of an existing entry exactly (same indentation).

### 3. "The arc" (career roadmap timeline)
- Each stage has a `phase` (e.g. "Near term", "Early career"), a `label` (short title), and a `detail` (longer description).
- Safe to change: the text in `label` and `detail` for any stage.

### 4. Closing line
- `closing` — the final sentence about the vineyard. Safe to edit.

## IMPORTANT — hard rules for this page
- The **BlackRock internship** stage must stay phrased softly — a family connection "opens the door," it does NOT say a job is guaranteed or secured. Keep that wording if you edit this section.
- Don't change "self-directed market study" type phrasing back to "self-taught" in a way that contradicts the family-mentorship story used elsewhere.

## No photos needed
This page is text-only.

## Nothing else needs fixing on this page
Layout and mobile display were checked and are working correctly, including the timeline on small screens.
